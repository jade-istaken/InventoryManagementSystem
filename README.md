# InventoryManagementSystem
An Inventory Management System made in C# for COMP586: Object Oriented Software Development 

Simply navigate to `src/` and run `dotnet clean && dotnet restore && dotnet build && dotnet run` and navigate to `localhost:5000` in your web browser.
